apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: traefik
  labels:
    app.kubernetes.io/name: traefik
spec:
  defaultCertificate:
    secretName: wildcard-apps-tls
