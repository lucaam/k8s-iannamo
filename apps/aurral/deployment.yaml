apiVersion: apps/v1
kind: Deployment
metadata:
  name: aurral
  namespace: aurral
  labels:
    app: aurral
    app.kubernetes.io/name: aurral
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aurral
      app.kubernetes.io/name: aurral
  template:
    metadata:
      labels:
        app: aurral
        app.kubernetes.io/name: aurral
    spec:
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
      - name: aurral
        image: ghcr.io/lklynet/aurral:1.76.3
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
        ports:
        - containerPort: 3001
          name: http
        env:
        - name: PORT
          value: "3001"
        - name: TRUST_PROXY
          value: "1"
        - name: DOWNLOAD_FOLDER
          value: "/app/downloads"
        - name: PUID
          value: "1001"
        - name: PGID
          value: "1001"
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: aurral-secrets
              key: ADMIN_PASSWORD
        - name: APP_DOCS_URL
          value: "https://github.com/lklynet/aurral"
        envFrom:
        - secretRef:
            name: aurral-secrets
        volumeMounts:
        - name: aurral-config
          mountPath: /app/config
        - name: aurral-media
          mountPath: /app/downloads
        - name: aurral-storage
          mountPath: /app/backend/data
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          tcpSocket:
            port: 3001
          initialDelaySeconds: 60
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          tcpSocket:
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 10
          failureThreshold: 3
      volumes:
      - name: aurral-config
        persistentVolumeClaim:
          claimName: aurral-config
      - name: aurral-media
        persistentVolumeClaim:
          claimName: aurral-media
      - name: aurral-storage
        persistentVolumeClaim:
          claimName: aurral-storage
