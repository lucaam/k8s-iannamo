apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - limitrange.yaml
  - helm-release.yaml
  # secret and HTTPRoute intentionally removed to keep FlareSolverr internal
metadata:
  name: flaresolverr
  labels:
    app.kubernetes.io/name: flaresolverr
