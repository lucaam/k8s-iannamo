apiVersion: v1
kind: Secret
metadata:
  name: transmission-secrets
  namespace: media
  labels:
    app.kubernetes.io/name: transmission-secrets
type: Opaque
sops:
  version: "3.11.0"
  # NOTE: secret values intentionally omitted here. Encrypt and add
  # them with `sops` (do not commit plaintext credentials).
