apiVersion: v1
kind: Secret
metadata:
  name: soulsync-secrets
  namespace: media
  labels:
    app.kubernetes.io/name: soulsync-secrets
type: Opaque
sops:
  version: "3.11.0"
  # NOTE: secret values intentionally omitted here. Encrypt and add
  # them with `sops` (do not commit plaintext credentials).
