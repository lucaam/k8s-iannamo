apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helm-release.yaml
  - pvc.yaml
  - secret-lidarr.sops.yaml
  - httproute.yaml
metadata:
  name: lidarr
  labels:
    app.kubernetes.io/name: lidarr
