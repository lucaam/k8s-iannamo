apiVersion: v1
kind: Service
metadata:
  name: soulsync
  namespace: soulsync
  labels:
    app.kubernetes.io/name: soulsync
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: soulsync
